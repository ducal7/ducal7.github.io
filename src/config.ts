// Site configuration.
//
// VISITOR ANALYTICS (optional, 2-minute setup — gives a REAL global page-view
// count on the live site):
//   1. Create a free account at https://www.goatcounter.com (privacy-first, no
//      cookies, GDPR-friendly). Pick a site code, e.g. `ducal7`.
//   2. In Settings → "Allow adding visitor counts on your own site", enable it.
//   3. Put your code below: GOATCOUNTER_CODE = 'ducal7'.
// Until then the "visitors" tile honestly shows *your* visit count for this
// browser (localStorage) instead of a fabricated global number.
export const GOATCOUNTER_CODE = 'ducal7'

export const GOATCOUNTER_ENABLED = GOATCOUNTER_CODE.length > 0
